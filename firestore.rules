rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // REGRAS TEMPORARIAMENTE PERMISSIVAS PARA DEBUG
    match /{document=**} {
      allow read, write: if true;
    }
  }
}